require "./spec_helper"

describe PageAnalizis do
  context ".parse" do
    pending "should respond with right data" do
      PageAnalizis.new({ "key" => "10" })
      PageAnalizis.parse("asdf")
    end
  end
end
